# .understandignore — 分析时要排除的文件/目录模式
# 语法：和 .gitignore 一样（glob、# 注释、! 取反、/ 结尾表示目录）
# 以下是建议项 — 取消注释即可激活。
# 用 ! 前缀可以强制包含被默认排除的内容。
#
# 内置默认排除项（除非用 ! 取反，否则始终生效）：
#   node_modules/, .git/, dist/, build/, obj/, *.lock, *.min.js 等
#
# --- 测试文件模式（取消注释即可排除） ---

# *.test.*
# *.spec.*
# *.snap
